# نظام إدارة الكاش التلقائي

## نظرة عامة
تم إضافة نظام تلقائي لإدارة كاش التطبيقات لضمان ظهور التحديثات فوراً على جميع الأجهزة بما في ذلك الهواتف المحمولة.

## كيف يعمل النظام

### 1. ربط الكاش برقم الإصدار
- يتم ربط اسم كاش Service Worker برقم إصدار المشروع من `package.json`
- عند تغيير رقم الإصدار، يتم إنشاء كاش جديد تلقائياً
- يتم حذف الكاش القديم تلقائياً بواسطة Service Worker

### 2. المشاريع المدعومة
- `frontend-worker-pwa` - تطبيق الموظفين (مع Service Worker)
- `frontend-admin-dashboard` - لوحة تحكم الإدارة
- `frontend-client-portal` - بوابة العملاء

## كيفية الاستخدام

### عند إجراء تحديثات مهمة:

**الخيار 1: تحديث يدوي بسيط**
```bash
# تغيير رقم الإصدار يدوياً في package.json
# ثم البناء
npm run build
```

**الخيار 2: تحديث تلقائي (موصى به)**
```bash
# هذا الأمر يزيد رقم الإصدار تلقائياً ويقوم بالبناء
npm run build:version
```

### أمثلة:

**لتطبيق الموظفين:**
```bash
cd frontend-worker-pwa
npm run build:version
```

**لوحة تحكم الإدارة:**
```bash
cd frontend-admin-dashboard
npm run build:version
```

**بوابة العملاء:**
```bash
cd frontend-client-portal
npm run build:version
```

## متى يجب تحديث رقم الإصدار؟

### يُنصح بالتحديث عند:
- تغيير شعار الشركة أو الصور
- تعديل النصوص العربية الأساسية
- تغيير ألوان التصميم
- إضافة ميزات جديدة
- إصلاح أخطاء مهمة
- تغيير الهيكل العام للتطبيق

### لا يحتاج تحديث عند:
- تصحيحات صغيرة في الكود
- تحسينات الأداء
- تغييرات غير مرئية للمستخدم

## ماذا يحدث للمستخدمين؟

### عند تحديث رقم الإصدار:
1. **على الحاسوب:** ستظهر التحديثات فوراً عند تحديث الصفحة
2. **على الهاتف (PWA):** 
   - سيتم تنظيف الكاش القديم تلقائياً
   - سيتم تحميل التطبيق الجديد عند فتحه
   - لا يحتاج المستخدم لتنظيف الكاش يدوياً

### السلوك المتوقع:
- **أول فتح بعد التحديث:** قد يستغرق التحميل وقتاً أطول قليلاً (تحميل المحتوى الجديد)
- **الفتحات التالية:** التطبيق سيعمل من الكاش الجديد بسرعة
- **لا حاجة للتدخل اليدوي:** المستخدم لا يحتاج لأي إجراءات

## إدارة الإصدارات

### نظام الترقيم:
- **Patch (1.0.1 → 1.0.2):** إصلاحات وتحسينات صغيرة
- **Minor (1.0.2 → 1.1.0):** ميزات جديدة
- **Major (1.1.0 → 2.0.0):** تغييرات كبيرة

### الأوامر المتاحة:
```bash
# زيادة رقم patch تلقائياً (الأكثر استخداماً)
npm version patch

# زيادة رقم minor
npm version minor

# زيادة رقم major
npm version major
```

## الملفات المعدلة

### 1. `package.json`
- تمت إضافة سكربت `build:version`
- تم تحديث رقم الإصدار إلى 1.0.1

### 2. `vite.config.js`
- تمت إضافة قراءة رقم الإصدار من `package.json`
- تمت إضافة plugin مخصص لمعالجة Service Worker

### 3. `public/service-worker.js` (فقط في worker-pwa)
- تم تغيير اسم الكاش لاستخدام متغير ديناميكي
- Service Worker يحذف الكاش القديم تلقائياً

## استكشاف الأخطاء

### إذا لم تظهر التحديثات:
1. تأكد من تغيير رقم الإصدار في `package.json`
2. تأكد من تشغيل `npm run build` بعد تغيير الإصدار
3. تحقق من Console في المتصفح لمعرفة ما إذا كان Service Worker يعمل
4. على الهاتف، أعد فتح التطبيق بعد التحديث

### للتحقق من الإصدار الحالي:
```bash
# عرض رقم الإصدار في package.json
cat package.json | grep version
```

## مزايا النظام الجديد

✅ **تحديثات فورية:** لا يحتاج المستخدمون لتنظيف الكاش يدوياً
✅ **سهولة الاستخدام:** أمر واحد للتحديث والبناء
✅ **تجربة مستخدم أفضل:** لا حاجة لإعادة تثبيت التطبيق
✅ **آمن:** يحافظ على الكاش القديم كنسخة احتياطية حتى يتم التأكد من نجاح التحديث
✅ **مرن:** يعمل على جميع المنصات (ويب، PWA، تطبيقات موبايل)